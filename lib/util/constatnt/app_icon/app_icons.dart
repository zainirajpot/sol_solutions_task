import 'package:flutter/material.dart';

class AppIcons {
  static const Icon notify = Icon(Icons.arrow_forward);
  static const location = Icon(Icons.location_on);
  static const forward = Icons.arrow_forward;
    static const add = Icons.add;

    static const rightIcons = Icons.keyboard_arrow_right;

}
